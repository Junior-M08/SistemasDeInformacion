@model IEnumerable<SistemasDeInformacioinMVC.NumeroDocumento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<body style="background-color:skyblue;">
</body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UltimoNumero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaRegistro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UltimoNumero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdNumeroDocumento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdNumeroDocumento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdNumeroDocumento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
